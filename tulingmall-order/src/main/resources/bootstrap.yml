spring:
  cloud:
    nacos:
      config:
        server-addr: 120.77.152.72:8848
        file-extension: yml
        shared-dataids: tulingmall-nacos.yml
        refreshable-dataids: tulingmall-nacos.yml
        max-retry: 10
  application:
    name: tulingmall-order
  profiles:
    active: dev

